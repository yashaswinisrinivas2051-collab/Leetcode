class Solution:
    def numSub(self, s: str) -> int:
        MOD = 10**9 + 7
        ans = 0
        count = 0
        for c in s:
            if c == '1':
                count += 1
            else:
                ans += count * (count + 1) // 2
                ans %= MOD
                count = 0
        ans += count * (count + 1) // 2
        ans %= MOD
        return ans
